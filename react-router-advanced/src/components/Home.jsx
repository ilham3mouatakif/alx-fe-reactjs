import React from 'react';

const Home = () => {
    return (
        <div>
            <h1>Welcome to React Router Advanced Demo</h1>
            <p>Explore nested, dynamic, and protected routes using the navigation links above.</p>
        </div>
    );
};

export default Home;
